{
  "name": "@prism/relay",
  "version": "0.1.0",
  "private": true,
  "description": "Prism E2EE relay — sovereign fork of paseo's zero-knowledge relay (ECDH + NaCl box). Powers off-LAN broker access.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "base64-js": "^1.5.1",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.0.0",
    "vitest": "^3.2.4"
  }
}
