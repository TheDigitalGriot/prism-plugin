# Prism 4.5.2

**Release date:** 2026-07 (backfilled 2026-07-22 from git — snapshot missed at release time; untagged)
**Type:** feature (Fragment inheritance)

## Summary

The **closing-ceremony meta-skill handed down to Fragment** — Fragment-scaffolded projects inherit the
bookend → docs → release ceremony.

## What changed

- Fragment templates now carry the closing-ceremony convention, so a scaffolded app ships with the same
  release discipline Prism uses (Prism is the source of truth; Fragment emits what Prism is).

*(Backfill note: reconstructed from commit `f42e10c`. Untagged release.)*
