---
date: 2026-07-17T09:00:00-04:00
researcher: Claude
git_commit: a0923b5 (main)
branch: main
topic: "Forge-day close — 4.2.0 → 4.3.1 arc, distribution cured, ecosystem hardened"
tags: [handoff, release, crlf, marketplace, mirror, agents, close-of-day]
status: complete
---

# Handoff: forge-day close (2026-07-17)

## What this day shipped (read CHANGELOG 4.2.0–4.3.1 for full detail)

- **v4.2.0** — Model B live: always-on daemon on the droplet (Coolify `prism:main-daemon`),
  relay-connected, 8 release assets. Conventions resumed (docs / evals / changelog).
- **v4.2.1** — POSIX-hardened all 5 sh hooks (cloud fail-close hotfix).
- **v4.3.0** — the TRUE root cause: **CRLF line endings** (`.gitattributes` eol=lf, disk
  renormalized, hooks.json `sh` runner, prepare-resources includes hooks+scripts). Full 8 assets.
  Release-process guards (Step 1c clean-tree — caught a live race on first run; bump-trap warning).
  Mid-task interjection protocol encoded in project + global CLAUDE.md.
- **v4.3.1** — orphan `prism-eval` gitlink untracked (marketplace `failed_content` suspect;
  timeline matches the whole stale-package saga since Jul 8).
- **Post-4.3.1 (commits abf3824, a0923b5):** bump-version idempotency verification (tested);
  `.easignore`; stale branch deleted; `<example>` blocks in all 14 agents (validator 14/14 +
  strict-YAML clean); **marketplace mirror** `TheDigitalGriot/prism-marketplace` created,
  `scripts/sync-marketplace.sh` + prism-release Step 6.5, **synced at v4.3.1**.

## Distribution state (all cured)

| Surface | State |
|---|---|
| Claude Desktop / Cowork | **4.3.1 via sideload upload** — cure verified in the previously-dead chat; CRLF root cause independently reproduced there |
| Claude Code (this machine) | **cache updated 3.9.5 → 4.3.1** (`claude plugin update prism@prism-marketplace`); applies on restart |
| Marketplace | full-monorepo sync still `failed_content` (backend size/content limit) → **the thin mirror is the new front door**: point Desktop's Add-marketplace at `TheDigitalGriot/prism-marketplace` |
| Cloud containers | prism currently absent from cloud sync (marketplace removal); OPEN — verify whether the uploaded 4.3.1 syncs into fresh containers |

## Open in other sessions (do not duplicate)

- **P1 (Cowork):** PASEO reference audit — spec in the 04-33-12 handoff.
- **P2:** Claude connector + artifact popout — research-first; head start = plugin MCP already
  surfaces in Desktop (brainstorm-channel on the 4.3.1 upload).
- Gavin's OneDrive/Claude-dir migration — his Cowork session owns it (incl. Known-Folder-Move
  un-redirect ordering; `.thedigitalgriot` is a LIVE daemon runtime — cloud write caution).

## Gavin's remaining hand-items

1. **Skill-guard allowlist patch** — 2 paste-blocks (in chat, 2026-07-17): `ALLOWLIST_RE` after
   `DELIVERABLE_EXT`, path check before the deliverable-extension gate. Classifier hard-blocks
   Claude from editing its own guard — by design.
2. Restart Claude Code sessions to load 4.3.1.
3. Next cloud task: check fresh-container plugin sync (see Distribution table).

## Parked (unchanged)

Model-B pairing "connecting" stall (findings in 04-33-12 handoff §Parked Findings) · Ideate/Design
implement-or-re-true decision · root `npm install` (needs deliberate node-22/24 window) · iOS
preview interactive rebuild · sovereign PASEO rename (awaits P1 audit greenlight).

## Standing lessons (now encoded, don't relearn)

LF contract (`.gitattributes` + POSIX hooks) · clean-tree guard before releases (parallel sessions
share this tree — P1/P2 own it as of this handoff; this session stopped writing) · never hand-edit
VERSION before bump-version.py · gitlinks never bare in a distributed tree · GH release assets
upload one-at-a-time · mid-task interjections = stop, answer, integrate, resume on go.
